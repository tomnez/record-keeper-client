{{#if model.students.content}}
<ul class="students-listing list-group">
  {{#each model.students.content as |student|}}
    <li class="list-group-item">{{student.first_name}} {{student.last_name}}</li>
  {{/each}}
</ul>
{{else}}
  <p>You have not added any students yet.</p>
{{/if}}
